// Immediately Invoked Function Expressions (IIFE)

// iife use when we need to call immediately function


(function code(){
    console.log("hello i am Lakshay");
})();

((name) => {
    console.log(`DB CONNECTED TWO ${name}`);
    
})();